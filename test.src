/* husuau dadsa */

program test_program is
    integer a;
    integer das;
    integer myarray[5];
    procedure f (integer x in, integer y in, integer sum out, integer mul out)
        integer b;
    begin
        b := 3+2*6+10;
        sum := 10;
        mul := 15;
        putInteger(x);
        if (b == x) then
            b := 40;
            putInteger(b);
        else
            b := 50;
            putInteger(y);
        end if;
    end procedure;

    procedure another (integer x2 out, integer yx out)
    begin
        x2 := 20;
        yx := 30;
    end procedure;

begin
    a := 5;
    das := 9;
    f(6, 10, a, das);
    putInteger(a);
    putInteger(das);
end program .
